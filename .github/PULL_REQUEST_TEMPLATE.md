## Quoi

## Pourquoi

## Checklist
- [ ] commits signés (`git commit -s`) et en Conventional Commits
- [ ] `cargo fmt` · `cargo clippy --all-targets -- -D warnings` · `cargo test` · `cargo deny check`
- [ ] nouveau cas de PDF → fixture + test
- [ ] si `fyp-plugin-api` est touché : compatible / cassant, version ajustée
