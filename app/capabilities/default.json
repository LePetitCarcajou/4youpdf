{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: invoke the application's commands and listen to window events (file drop).",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
