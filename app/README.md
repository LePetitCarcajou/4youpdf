# app — application desktop (jalon 0.3)

Tauri 2 + TypeScript. Initialisation prévue avec :

```
cargo tauri init
```

Ne pas commencer avant que `fyp-core` passe le round-trip sur le corpus :
l'interface s'accroche à un noyau stable, pas l'inverse.
